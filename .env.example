# Optional. The game ships with ECHO's own Supabase project built in.
# Point these at your own project to host a separate leaderboard,
# or set NEXT_PUBLIC_SUPABASE_URL to an empty value to keep scores in the browser only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
